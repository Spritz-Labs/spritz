# Spritz E2E Test Configuration
# Copy to .env and fill in values

BASE_URL=https://app.spritz.chat

# Session secret (from main app .env — used to mint JWTs for test accounts)
SESSION_SECRET=

# Test wallet (for SIWE auth tests)
TEST_WALLET_PRIVATE_KEY=0x...
TEST_WALLET_ADDRESS=0xtest_wallet_spritz_e2e_001

# Test email account address
TEST_EMAIL_ADDRESS=0xtest_email_spritz_e2e_002

# Test passkey account address
TEST_PASSKEY_ADDRESS=0xtest_passkey_spritz_e2e_003

# Developer API key (auto-approved, for SDK-style tests)
TEST_API_KEY=sk_test_spritz_e2e_...

# Supabase anon key (for security tests — verifying columns are blocked)
SUPABASE_URL=https://vitcsvjssnxtncvtkmqq.supabase.co
SUPABASE_ANON_KEY=
