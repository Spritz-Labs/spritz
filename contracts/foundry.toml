[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

# Optional: forge script --rpc-url mainnet
[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
